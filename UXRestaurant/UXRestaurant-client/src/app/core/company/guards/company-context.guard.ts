// core/company/guards/company-context.guard.ts
//!import { inject } from '@angular/core';
import { CanActivateFn, Router } from '@angular/router';
//import { CompanyFacadeService } from '../services/company-facade.service';

//export const companyContextGuard: CanActivateFn = () => {
//   const facade = inject(CompanyFacadeService);
// const router = inject(Router);

//if (facade.activeCompanyId()) {
// return true;
// }

// return router.createUrlTree(['/companies/select']);
//};