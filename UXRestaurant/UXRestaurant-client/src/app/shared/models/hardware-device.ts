export interface HardwareDevice {
    id: string;
    icon: string; // placeholder por agora
    name: string;
    description: string;
}

export interface CompatiblePartner {
    id: string;
    name: string;
    logo: string;
}